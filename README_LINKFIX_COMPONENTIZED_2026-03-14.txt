Deze build bevat:
- hele map automatisch gefixt
- alle links herschreven naar werkende root-routes
- navbar en footer gecomponentiseerd via /assets/js/components.js
- 404.html toegevoegd

Lokale test:
cd C:\NEN1090\NEN1090-marketing
npx http-server -p 8080

GitHub push lokaal:
git add .
git commit -m "Marketing: fix all links and componentize navbar"
git push
