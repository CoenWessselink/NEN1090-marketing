name: Deploy API to Azure (clean)

on:
  workflow_dispatch:

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Stop wrong-repo API deployment
        run: |
          echo "Deze workflow is bewust gedeactiveerd."
          echo "API deploy hoort NIET in NEN1090-marketing."
          echo "Gebruik de workflow in repo: CoenWessselink/NEN10900-api"
